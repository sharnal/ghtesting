name: conditional pull request
on:
  pull_request
jobs:
  job01:
    runs-on: ubuntu-latest
    steps:
      - name: First step
        run: echo "First Step"
      - name: Second step
        run: echo "Second Step"
  job02:
    runs-on: ubuntu-latest
    steps:
      - name: Third step
        run: echo "Third Step"
      - name: Fourth step
        run: echo "${{ toJSON(github.event.label.name) }}"
